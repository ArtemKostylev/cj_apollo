import styled from 'styled-components';

export const ControlItemButton = styled.button`
  border: 0;
  background-color: transparent;
  padding: 1em 0;
  cursor: pointer;
  width: 100%;

  &:hover {
    background-color: #e6eaea;
  }

  &:focus {
    outline: 0;
  }
`