import {css} from 'styled-components';
import {theme} from '../../../styles/theme';

export const errorStyles = css`
  border: 2px solid ${theme.error} !important;
  background-color: #FEC0AE !important;
`;