import styled from 'styled-components';

export const GroupHeaderWrapper = styled.tr`
  border: none;
  font-weight: normal;

  th {
    background-color: transparent;
    border: 0;
  }

  div {
    display: flex;
    flex-direction: row;
  }

  p {
    margin: 0 3rem;
  }
`;